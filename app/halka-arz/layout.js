/** Bu segment CDN / tarayıcı önbelleğinde eski HTML kalsın diye zorla dinamik. */
export const dynamic = 'force-dynamic';
export const revalidate = 0;

export default function HalkaArzLayout({ children }) {
  return children;
}
